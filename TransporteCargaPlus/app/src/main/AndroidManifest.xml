<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="unipiloto.edu.transportecargaplus"
    tools:ignore="CoarseFineLocation">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.hardware.location.gps"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TransporteCargaPlus" >

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Conductor.MapsActivity"
            android:exported="true"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".Conductor.Viajes"
            android:exported="true" />
        <activity
            android:name=".Conductor.MainConductor"
            android:exported="true" />
        <activity
            android:name=".Cliente.HistorialCarga"
            android:exported="true" />
        <activity
            android:name=".Cliente.InformacionCarga"
            android:exported="true" />
        <activity
            android:name=".Propietario.AsignacionCarga"
            android:exported="true" />
        <activity
            android:name=".Propietario.AsignarConductor"
            android:exported="true" />
        <activity
            android:name=".Cliente.SolicitarTransporte"
            android:exported="true" />
        <activity
            android:name=".Cliente.MainCliente"
            android:exported="true" />
        <activity
            android:name=".Propietario.AgregarCarro"
            android:exported="true" />
        <activity
            android:name=".Propietario.MainPropietario"
            android:exported="true" />
        <activity
            android:name=".Main.NewUser"
            android:exported="true" />
        <activity
            android:name=".Main.Login"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>